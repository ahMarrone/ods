{% extends 'base.html.twig' %}

{% block body %}
    <ol class="breadcrumb">
        <li><a href="{{ path('paneluser_index') }}">Panel de usuario</a></li>
        <li class="active">Objetivos</a></li>
    </ol>

    <div class="page-header">
          <h1>Detalle de Objetivo</h1>
    </div>

<dl class="dl-horizontal">
  <dt>Id</dt>
  <dd>{{ objetivo.id }}</dd>
</dl>
<dl class="dl-horizontal">
  <dt>Descripcion</dt>
  <dd>{{ objetivo.descripcion }}</dd>
</dl>


    <ul>
        <li>
            <a href="{{ path('admin_crud_objetivos_index') }}">Volver atrás</a>
        </li>
        <li>
            <a href="{{ path('admin_crud_objetivos_edit', { 'id': objetivo.id }) }}">Editar</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>

    <script type="text/javascript">
    $(document).ready(function() {
    $('#metasTable').DataTable({
         "language": {
            "url": "/lang/datatables_spanish.json"
        }
    });
} );
</script>
{% endblock %}
