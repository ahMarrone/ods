{% extends 'base.html.twig' %}

{% block body %}
    <ol class="breadcrumb">
        <li><a href="{{ path('paneluser_index') }}">Panel de usuario</a></li>
        <li><a href="{{ path('admin_crud_objetivos_index') }}">Objetivos</a></li>
        <li class="active">Detalle de objetivo</a></li>
    </ol>

    <div class="page-header">
          <h1>Detalle de Objetivo</h1>
    </div>

<dl class="dl-horizontal">
  <dt>Id</dt>
  <dd>{{ objetivo.id }}</dd>
</dl>
<dl class="dl-horizontal">
  <dt>Descripcion</dt>
  <dd>{{ objetivo.descripcion }}</dd>
</dl>

<div class="row">
            <a class="col-md-1 btn btn-primary option-btn"  href="{{ path('admin_crud_objetivos_index') }}">Volver atrás</a>
            <a class="col-md-1 btn btn-primary option-btn"  href="{{ path('admin_crud_objetivos_edit', { 'id': objetivo.id }) }}">Editar</a>
            {{ form_start(delete_form) }}

                <input class="btn btn-danger col-md-1 option-btn" type="submit" value="Eliminar">
            {{ form_end(delete_form) }}
</div>

    <script type="text/javascript">
    $(document).ready(function() {
    $('#metasTable').DataTable({
         "language": {
            "url": "/lang/datatables_spanish.json"
        }
    });
} );
</script>
{% endblock %}
