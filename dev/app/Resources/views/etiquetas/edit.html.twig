{% extends 'base_panel_users.html.twig' %}

{% block panel_body %}
    {% if  show_breadcrumbs is defined and show_breadcrumbs == true %} 
    <ol class="breadcrumb">
        <li><a href="{{ path('paneluser_index') }}">Panel de usuario</a></li>
        <li><a href="{{ path('admin_crud_desgloces_index') }}">Etiquetas</a></li>
        <li class="active">Edición de etiqueta</a></li>
    </ol>
    {% endif %}

     <div class="page-header">
          <h2>Editar Etiqueta</h2>
    </div>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="col-md-1 btn btn-success option-btn" type="submit" value="Guardar" />
    {{ form_end(edit_form) }}

<div class="row">
            {{ form_start(delete_form) }}
                <input class="col-md-1 btn btn-danger option-btn" type="submit" value="Eliminar">
            {{ form_end(delete_form) }}
</div>

<br><br><br><br><br>

{% endblock %}
